<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SpaceX Launch Tracker</title>
    <link rel="stylesheet" href="/styles/main.css">
</head>
<body>
    <div class="container">
        <h1>Latest SpaceX Launch 🚀</h1>

        <% if (locals.error) { %>
            <div class="error-box">
                <h3>⚠️ حدث خطأ!</h3>
                <p><%= error %></p>
            </div>
        <% } 
        /* التحقق: هل وصلت البيانات؟ */
        else if (locals.data) { %>
            <div class="card">
                <% if (data.links.patch.small) { %>
                    <img src="<%= data.links.patch.small %>" alt="Mission Patch">
                <% } %>
                
                <h2>Mission: <%= data.name %></h2>
                <p><strong>Date:</strong> <%= new Date(data.date_utc).toDateString() %></p>
                
                <p class="details">
                    <%= data.details ? data.details : "No details available for this mission." %>
                </p>
            </div>
        <% } %>
    </div>
</body>
</html>